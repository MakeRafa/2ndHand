<div class="container-fluid d-flex justify-content-center pt-5 ">

    <img class="w-25 h-100 mx-5" src="images/online_learning.jpg" style="height:250px; margin-left:-15px;" />

    {{!-- <div class="col-xs-6">

        <h3 class="title">{{post.title}}</h3>
        <h5 class="price">Price: ${{post.price}}</h5>
        <h5 class="size">Size: {{post.size}}</h5>
        <p class="description" style="font-size:10px; color:#03225C;">{{post.description}}</p>

    </div> --}}
    <div class="card h-100 w-25 mx-5">
        {{!-- <img src="{{this.image}}" class="card-img-top" alt="..."> --}}
        <div class="card-body">
            <h5 class="seller">{{post.creator.username}}</h5>
            <h5 class="title d-flex justify-content-center">{{post.title}}</h5>
        </div>
        <ul class="card-list-group list-group-flush">
            <li class="list-group-item">Description: {{post.description}}</li>
            <li class="list-group-item">Size: {{post.size}}</li>
            <li class="list-group-item">Price: ${{post.price}}</li>
        </ul>
        <div class="card-body d-flex justify-content-around">
            <a href="/" class="btn btn-primary">Go Back</a>
            {{#if currentUser}}
            <a href="/posts/{{post._id}}" class="btn btn-danger">Edit</a>

            <a href="#" class="btn btn-success">Add to Hand</a>
            {{/if}}
        </div>
    </div>
</div>